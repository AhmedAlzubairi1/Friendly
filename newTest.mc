make_a function called main outputting number using () does {
  make_a float called a.
  make_a bool called b.
  make_a sentence called c.
  make a be 100.5.
  make b be true.
  make c be "Hello World".
  do showWords(c).
  do showBool(b && false).
  do showFloat(a + a + 1.0).
   return 0. 
 }
